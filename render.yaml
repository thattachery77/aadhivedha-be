services:
  - type: web
    name: aadhivedha
    env: java
    plan: free
    buildCommand: chmod +x gradlew && ./gradlew clean build
    startCommand: java -jar build/libs/adhivedha-1-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 21.0.6
